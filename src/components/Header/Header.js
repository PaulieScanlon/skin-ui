/** @jsx jsx */
import { jsx } from "theme-ui"

export const Header = () => (
  <header
    sx={{
      alignItems: "center",
      borderBottomStyle: 0,
      borderBottomColor: "surface",
      borderBottomWidth: 1,
      backgroundColor: "background",
      display: "flex",
      height: 1,
      p: 2,
      position: "fixed",
      width: 0,
      zIndex: 0,
    }}
  >
    nude-ui
  </header>
)
